stages:
  - build
  - deploy

build-frontend:
  image: node
  stage: build
  script:
    - pwd
    - cd frontend/
    - npm install
    - npm run test -- --watchAll=false
    - npm run build
  artifacts:
    paths:
      - "build/"

build-backend:
  image: node
  stage: build
  script:
    - pwd
    - ls -la build/
    - cd backend/
    - ls -la build/
  